import topicReducer from '../src/reducers/topicSlice';

describe('reducer tests', () => {
  describe('topicSlice.js', () => {
    test('activate reducer works as expected', () => {
      console.log(topicReducer);
    });
  });
});
